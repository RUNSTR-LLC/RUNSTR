/**
 * RUNSTR App Entry Point
 * Standard React Native entry point without Expo Router
 */

import { registerRootComponent } from 'expo';
import App from './src/App';

// Register the main component
registerRootComponent(App);